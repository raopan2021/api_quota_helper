# 01-开发环境搭建与第一个3D场景

本章节将手把手教你搭建 Three.js 开发环境，并创建一个最简单的 3D 场景。

## 📋 目录
1. [环境准备](#环境准备)
2. [项目初始化](#项目初始化)
3. [第一个3D场景](#第一个3d场景)
4. [代码详解](#代码详解)
5. [运行与调试](#运行与调试)
6. [练习作业](#练习作业)

---

## 环境准备

### 方式一：使用 CDN（推荐入门）

创建一个 HTML 文件即可，无需安装任何依赖。

```html
<!-- 01-first-scene.html -->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Three.js - 第一个3D场景</title>
    <style>
        /* 清除默认边距，让画布全屏显示 */
        body {
            margin: 0;
            overflow: hidden;
        }
        canvas {
            display: block;
        }
    </style>
</head>
<body>
    <script type="importmap">
    {
        "imports": {
            "three": "https://unpkg.com/three@0.160.0/build/three.module.js"
        }
    }
    </script>
    <script type="module">
        import * as THREE from 'three';

        // ========== 场景创建 ==========
        // 场景是所有 3D 对象的容器
        const scene = new THREE.Scene();
        
        // 设置场景背景颜色（可选）
        scene.background = new THREE.Color(0x1a1a2e);

        // ========== 相机创建 ==========
        // PerspectiveCamera：透视相机，模拟人眼视觉
        // 参数：(FOV角度, 近宽比, 近裁面, 远裁面)
        const camera = new THREE.PerspectiveCamera(
            75,                                    // FOV：视野角度
            window.innerWidth / window.innerHeight, // 宽高比
            0.1,                                   // 近裁面：小于此距离不渲染
            1000                                   // 远裁面：大于此距离不渲染
        );
        
        // 相机位置：x=0, y=0, z=5（暂时理解为眼睛的位置）
        camera.position.set(0, 0, 5);

        // ========== 渲染器创建 ==========
        // WebGLRenderer：使用 WebGL 绘制场景
        const renderer = new THREE.WebGLRenderer({ antialias: true });
        
        // 设置渲染器尺寸
        renderer.setSize(window.innerWidth, window.innerHeight);
        
        // 设置像素比（高分屏适配）
        renderer.setPixelRatio(window.devicePixelRatio);
        
        // 将渲染器添加到页面中
        document.body.appendChild(renderer.domElement);

        // ========== 添加3D物体 ==========
        // 创建一个立方体几何体
        const geometry = new THREE.BoxGeometry(1, 1, 1);
        
        // 创建材质（控制物体外观）
        const material = new THREE.MeshBasicMaterial({ 
            color: 0x00ff88,
            wireframe: false  // true=线框模式，false=实体模式
        });
        
        // 创建网格物体（结合几何体和材质）
        const cube = new THREE.Mesh(geometry, material);
        
        // 将物体添加到场景中
        scene.add(cube);

        // ========== 动画循环 ==========
        function animate() {
            requestAnimationFrame(animate);  // 请求下一帧动画
            
            // 让立方体旋转（数值越大速度越快）
            cube.rotation.x += 0.01;
            cube.rotation.y += 0.01;
            
            // 执行渲染
            renderer.render(scene, camera);
        }
        
        // 启动动画
        animate();

        // ========== 响应窗口大小变化 ==========
        window.addEventListener('resize', () => {
            // 更新相机宽高比
            camera.aspect = window.innerWidth / window.innerHeight;
            // 更新相机投影矩阵
            camera.updateProjectionMatrix();
            // 更新渲染器尺寸
            renderer.setSize(window.innerWidth, window.innerHeight);
        });
    </script>
</body>
</html>
```

---

## 项目初始化

### 方式二：使用 Vite 构建工具（推荐正式项目）

```bash
# 1. 创建项目目录
mkdir my-threejs-project
cd my-threejs-project

# 2. 初始化 npm 项目
npm init -y

# 3. 安装 Three.js
npm install three

# 4. 安装 Vite 开发服务器
npm install -D vite

# 5. 创建 index.html
```

### Vite 项目结构

```
my-threejs-project/
├── index.html
├── package.json
├── vite.config.js
└── src/
    └── main.js
```

### vite.config.js 配置

```javascript
// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
    server: {
        port: 3000,        // 开发服务器端口
        open: true         // 自动打开浏览器
    },
    build: {
        outDir: 'dist',   // 输出目录
        sourcemap: true   // 生成 source map
    }
});
```

### src/main.js（Vite 项目入口）

```javascript
// src/main.js
import * as THREE from 'three';

// 1. 创建场景
const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1a2e);

// 2. 创建相机
const camera = new THREE.PerspectiveCamera(
    75,
    window.innerWidth / window.innerHeight,
    0.1,
    1000
);
camera.position.set(0, 0, 5);

// 3. 创建渲染器
const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.setPixelRatio(window.devicePixelRatio);
document.body.appendChild(renderer.domElement);

// 4. 添加几何体
const geometry = new THREE.BoxGeometry(1, 1, 1);
const material = new THREE.MeshBasicMaterial({ color: 0x00ff88 });
const cube = new THREE.Mesh(geometry, material);
scene.add(cube);

// 5. 动画循环
function animate() {
    requestAnimationFrame(animate);
    cube.rotation.x += 0.01;
    cube.rotation.y += 0.01;
    renderer.render(scene, camera);
}
animate();

// 6. 响应窗口变化
window.addEventListener('resize', () => {
    camera.aspect = window.innerWidth / window.innerHeight;
    camera.updateProjectionMatrix();
    renderer.setSize(window.innerWidth, window.innerHeight);
});
```

### package.json 添加脚本

```json
{
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview"
    }
}
```

---

## 代码详解

### 三大核心组件

Three.js 的 3D 场景由三个核心组件构成：

| 组件 | 类名 | 作用 |
|------|------|------|
| 场景 | `THREE.Scene` | 存放所有 3D 对象的容器，类似虚拟世界 |
| 相机 | `THREE.Camera` | 决定观察视角，类似人眼或摄像机 |
| 渲染器 | `THREE.WebGLRenderer` | 将场景绘制到屏幕上，类似胶片或显示屏 |

### 执行流程图

```
┌─────────────────────────────────────────────────────┐
│                    初始化阶段                         │
├─────────────────────────────────────────────────────┤
│  1. 创建 Scene（场景容器）                           │
│  2. 创建 Camera（决定视角）                          │
│  3. 创建 Renderer（绑定到 Canvas）                   │
│  4. 创建 Mesh（几何体+材质）并加入 Scene              │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│                    渲染循环                          │
├─────────────────────────────────────────────────────┤
│  1. requestAnimationFrame 触发                       │
│  2. 更新物体状态（位置/旋转/缩放）                    │
│  3. renderer.render(scene, camera) 执行渲染          │
│  4. 循环回第1步                                       │
└─────────────────────────────────────────────────────┘
```

### 坐标系说明

Three.js 默认使用右手坐标系：
- **X 轴**：向右为正方向
- **Y 轴**：向上为正方向
- **Z 轴**：垂直屏幕向外为正方向

```
        Y (上)
        ↑
        │
        │
        │______→ X (右)
       ╱
      ╱
     ↙ Z (朝外)
```

### 相机位置与观察目标

默认情况下，相机观察原点 (0, 0, 0)。可以通过 `camera.lookAt()` 改变观察目标：

```javascript
// 相机看向 (1, 1, 1) 点
camera.lookAt(1, 1, 1);

// 或者使用 setFromCamera + Vector2
const vector = new THREE.Vector2(0, 0);  // 屏幕中心
camera.setFromCamera(vector, camera);
```

---

## 运行与调试

### 运行开发服务器

```bash
# Vite 项目
npm run dev

# 或直接用 Python（无构建工具时）
python3 -m http.server 8080
```

### 打开调试工具

1. 浏览器按 F12 打开开发者工具
2. Console 面板查看日志输出
3. Elements 面板查看 Canvas 元素
4. Performance 面板分析帧率

### 常见错误

#### 错误1：Canvas 全屏但无内容
```javascript
// ❌ 错误：忘了添加到 body
const renderer = new THREE.WebGLRenderer();
document.body.appendChild(renderer.domElement);  // 这行不能少
```

#### 错误2：物体看不见
```javascript
// ❌ 错误：物体在相机后面
camera.position.set(0, 0, 0);  // 相机和物体重叠
cube.position.set(0, 0, 0);

// ✅ 解决：确保物体在相机视野内
camera.position.set(0, 0, 5);
cube.position.set(0, 0, 0);
```

#### 错误3：画面模糊
```javascript
// ❌ 错误：高分屏未设置像素比
renderer.setSize(window.innerWidth, window.innerHeight);

// ✅ 解决：设置像素比
renderer.setPixelRatio(window.devicePixelRatio);
```

---

## 练习作业

### 初级练习
1. 将立方体颜色改为红色（`0xff0000`）
2. 将立方体改为球体（使用 `SphereGeometry`）
3. 添加第二个立方体，放在第一个旁边

### 中级练习
1. 实现鼠标滚轮缩放功能
2. 添加键盘控制立方体移动
3. 实现物体随时间缩放呼吸效果

### 代码提示

#### 球体几何体
```javascript
// SphereGeometry(radius, widthSegments, heightSegments)
const sphere = new THREE.SphereGeometry(1, 32, 32);
```

#### 键盘控制
```javascript
const keys = {};
window.addEventListener('keydown', (e) => keys[e.code] = true);
window.addEventListener('keyup', (e) => keys[e.code] = false);

function animate() {
    if (keys['ArrowLeft']) cube.position.x -= 0.1;
    if (keys['ArrowRight']) cube.position.x += 0.1;
    // ...
}
```

---

## 下一步

下一章节：[02-坐标系与基础图形](./02-坐标系与基础图形.md)

---

*💡 提示：在编写代码时，多查看 [Three.js 官方文档](https://threejs.org/docs/)，它是最好的参考资料。*
